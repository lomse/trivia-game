version: '3.8'

services:

  guess-game:
    container_name: guess-game
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '4000:80'
    env_file:
      - .env